name: Regenerate adrs/000-ledger.md

on:
  push:
    branches: [main]
    paths:
      - 'adrs/000-ledger.json'

permissions:
  contents: write

jobs:
  regen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install deps
        run: pip install jsonschema
      - name: Render ledger
        run: python __SKILL_PATH__/scripts/render_ledger.py adrs/000-ledger.json
      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add adrs/000-ledger.json adrs/000-ledger.md
          if git diff --quiet --cached; then
            echo "no changes"
          else
            git commit -m "auto: regenerate adrs/000-ledger.md [skip ci]"
            git push
          fi
